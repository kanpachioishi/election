# Governor Page Runbook

最終更新: 2026-04-15 JST

このメモは、知事選の専用ページを追加するときの運用ルールをまとめたものです。
新潟県知事選ページと滋賀県知事選ページで固めた実務を、次の知事選へ横展開しやすい形にしています。

## 1. いつ専用ページを作るか

- メインページの公式リンク一覧だけでは足りず、候補予定者のプロフィールや出馬動向を1ページにまとめたいとき
- 告示前で、公式候補者一覧はまだ無いが、報道や本人発信で候補予定者をある程度確認できるとき
- 候補予定者と関連動向を、混同せずに見せる必要があるとき

逆に、候補予定者の確認がほぼ進んでいない段階では、専用ページを急いで作らず、まず `election_resource_links` の公式導線を固める。

## 2. 追加するファイル

最低限、次の 6 つをそろえる。

1. `data/v1/elections.json`
2. `data/v1/election_resource_links/{election_id}.json`
3. `data/v1/candidate_signals/{election_id}.json`
4. `data/v1/candidate_profiles/{election_id}.json`
5. `site/elections/{slug}.html`
6. `site/assets/{slug}.js`

必要に応じて追加するもの:

- `data/v1/candidate_endorsements/{election_id}.json`
- `site/assets/{slug}.css`
- `site/assets/app.js` の専用ページ導線

新潟・滋賀のように知事選ごとの見せ方が近い場合は、直前の知事選ページを複製してから、`ELECTION_ID` と文言を差し替えるのが早い。

## 3. データの基本ルール

### 3.1 `elections.json`

- `page_status` を必ず入れる
- 告示前は `official_candidate_list_status = "not_included"` を使う
- `page_updates` に、専用ページを追加した日付と要約を残す
- `verification.source_url` には、投票日・告示日を確認した公式ページか公式 PDF を入れる

### 3.2 `candidate_signals`

- 候補予定者・出馬動向はここに入れる
- `status = announced` は、本人表明か、それに準ずる強い報道があるときに使う
- `interested` や `related_interest` 相当の人物は、候補予定者一覧には混ぜない
- 根拠は最低 1 件、できれば 2 件以上あるとよい

### 3.3 `candidate_profiles`

- 役割は、評価ではなく「見分けるための事実整理」
- `person_kana` は、候補が少人数でも基本的に入れる
- `birth_date` は確実な出典があるときだけ入れる
- 年齢は手入力せず、`birth_date` から UI 側で計算する
- `identity_labels` は `現職知事`, `県議`, `元市長`, `県公式プロフィールあり` のような中立ラベルに限る
- 思想、色分け、推薦状況を `identity_labels` に混ぜない

### 3.4 `candidate_endorsements`

- 推薦・支持・擁立は、候補動向と混ぜず別ファイルに分ける
- まだ情報が薄い段階なら、無理に作らなくてよい

## 4. 候補予定者と関連動向の分け方

- `reported_candidate`, `announced`, `official_candidate` は候補予定者側へ出す
- `related_interest`, `interested` は `関連動向` セクションへ分ける
- 関連動向の人物を、比較帯、候補予定者プロフィール、公式導線比較に混ぜない
- 告示前は特に、「この人も候補かもしれない」と読める見出しや文言を避ける

## 5. プロフィール運用ルール

- まず「この人は誰か」が分かることを優先する
- 優先して集める導線は `公式・本人サイト / SNS / プロフィール / 政策`
- 公式サイトが無い人物は、本人ブログや本人 SNS を `本人発信` として扱ってよい
- 「本人発信が無いからやる気がない」と読める構成は避ける
- 公式サイトと本人ブログが実質同じ役割なら、無理に分離しない

肩書確認のルール:

- 現職肩書きは、古いプロフィールだけで断定しない
- 確認順は、直近選挙結果、議会や自治体の現職名簿、本人または党の最新プロフィール、報道
- 断定できない場合は `前衆院議員`, `元知事`, `元市議` のように現在形ではない表現へ逃がす

## 6. 見出しと文言

- 告示前は `候補予定者プロフィール（報道ベース）` を使う
- 公式候補者一覧が公表されたら `公式候補者プロフィール` に切り替える
- `official_candidate_list_status` の切り替えと、見出し・説明文の切り替えは同時に行う
- ページ冒頭では「公式情報と報道ベースを分けて整理している」と分かるようにする
- 説明は重複させすぎず、注意文・ステータス・比較帯注記の役割を分ける

## 7. UI の基本ルール

- 主役は候補者名。名前を先に読めるサイズにする
- ひらがな表記を付ける
- 年齢は名前の横に出すが、名前より目立たせない
- 比較帯では、まず `現職 / 新人` と肩書が一瞬で分かることを優先する
- 推薦・支持は `政党系 / 首長系 / 労組系 / その他` のように軽く束ねる
- 色や記号で思想や立場を想起させる見せ方は避ける

## 8. メインページとの役割分担

- メインページは公式リンク中心
- 専用ページは、報道ベースの候補予定者、プロフィール、時系列、出典を受け持つ
- `site/assets/app.js` には、その選挙だけ専用ページへ飛ぶ導線を足す
- 文言は `報道ベースの候補予定者ページへ` など、専用ページの役割が分かるものにする

## 9. 実装後の確認

最低限、次を通す。

```bash
node scripts/validate-data-v1.mjs
node scripts/generate-site-data.mjs
node --check site/assets/app.js
node --check site/assets/{slug}.js
```

見た目確認も必須:

- PC の実画面スクリーンショット
- モバイルの実画面スクリーンショット
- 必要なら縦長モバイルでも下部セクションまで確認

確認ポイント:

- 上部で「何のページか」がすぐ分かる
- 比較帯、プロフィール、公式情報、出典が崩れていない
- モバイルで横スクロールが出ない
- 日本語の改行が不自然でない
- 関連動向が候補予定者に混ざっていない

## 10. 外部リンク運用ルール

- 新しく追加した外部リンクは、公開前に必ず実リンク確認する
- `candidate_signals`, `candidate_profiles`, `candidate_endorsements`, `election_resource_links` の URL は、少なくとも一度はブラウザまたは同等手段で開いて確認する
- リンク先が 404, リダイレクトループ, アクセス不能, 別記事化している場合は、その URL を採用しない
- 転載・ミラー・まとめサイトより、元の発信元を優先する
- やむを得ず転載・配信先を使う場合は、記事詳細 URL よりも安定したトピック URL や発信元ページを優先する
- 「存在しそう」では登録しない。公開前に開けなかった URL は、いったん載せない
- URL を差し替えたら、`last_checked_at` か関連レコードの確認日時も同時に更新する
- URL を追加・修正したら、そのまま終わらせず `node scripts/generate-site-data.mjs` まで実行して、配信用データへ反映する
- 生成後は `site/data/site-data.js` に古い URL が残っていないか確認してから完了にする

特に信頼に関わるので、候補予定者ページの出典リンクは「クリックして開く」確認を省略しない。

## 11. 公式候補者一覧が出たあとの切り替え

1. `page_status.official_candidate_list_status` を `published` にする
2. `page_status.official_candidate_list` に公式 URL と確認日時を入れる
3. 見出しを `公式候補者` ベースに切り替える
4. `candidate_signals.status = official_candidate` へ必要分を更新する
5. `page_updates` に切り替え履歴を追加する
6. validate / generate / 実画面確認をやり直す

## 12. 迷ったときの優先順位

1. 誤認を防ぐ
2. 公式情報と報道ベースを混ぜない
3. 候補者を見分けやすくする
4. 更新しやすいデータ構造を守る
5. 装飾より可読性を優先する
